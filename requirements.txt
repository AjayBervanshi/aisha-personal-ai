# Aisha - Python Dependencies
# Install: pip install -r requirements.txt

# Core AI
google-genai>=1.0.0             # Gemini AI — new SDK (primary)
google-generativeai>=0.7.0      # Gemini AI — legacy SDK fallback
anthropic>=0.40.0               # Claude Opus 4.6 (with adaptive thinking + streaming)
groq>=0.9.0                     # Groq + Llama — ultra-fast backup brain
openai>=1.0.0                   # OpenAI (ChatGPT) + xAI Grok (OpenAI-compatible)
schedule>=1.2.0                 # Autonomous Cron Engine

# Telegram Bot
pyTelegramBotAPI>=4.20.0        # Telegram bot framework

# Database
supabase>=2.0.0                 # Supabase client for memory

# Utilities
python-dotenv>=1.0.0            # Environment variable management
httpx>=0.27.0                   # HTTP client (used internally)
pydantic>=2.0.0                 # Data validation
fastapi>=0.111.0                # Web API Backend for Lovable App
uvicorn>=0.30.0                 # Web Server for API

# Voice
elevenlabs>=1.0.0               # ElevenLabs TTS — Aisha + Riya voice narration
edge-tts>=6.1.9                 # Microsoft Edge TTS — free fallback voice engine
requests>=2.31.0                # HTTP requests (trend engine, analytics, APIs)

# Video Rendering
moviepy>=1.0.3                  # Audio + image → MP4 stitching
Pillow>=10.0.0                  # Image processing for Ken Burns effect
numpy>=1.24.0                   # Array ops for Ken Burns frame transforms

# Trend Research
pytrends>=4.9.2                 # Google Trends (free, no API key)

# YouTube OAuth + Analytics
google-auth-oauthlib>=1.2.0     # OAuth 2.0 flow for YouTube
google-api-python-client>=2.120.0  # YouTube Data API v3 + Analytics API
google-auth-httplib2>=0.2.0     # HTTP transport for Google auth

# Development
pytest>=7.0.0                   # Testing
python-json-logger>=2.0.0       # Structured logging

# API Security & Rate Limiting
slowapi>=0.1.9                  # Rate limiting middleware for FastAPI

# News Aggregation
newsapi-python>=0.2.6           # NewsAPI client (free tier: 100 req/day)

# Mistral AI (lazy-imported in ai_router.py)
mistralai>=1.0.0                # Mistral Large via direct API
